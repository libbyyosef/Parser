// double_tests.sjava: checks multiple possible double assignments(with/without ., +. -).
// should return 0.

void test() {
   double a = 1.3, b = .0, c=.3, d = -.3, e=+.3, f=3;
   double g = -1.3, h = -1, i = 0.3, j = 0;
   return;
}